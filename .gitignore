node_modules
.DS_Store
*.log

# Build output
/build
/.cache

# Environment — never commit real credentials
.env
.env.local

# Prisma local database
/prisma/dev.db
/prisma/dev.db-journal

# Shopify CLI
.shopify
.graphql_client_cache

# TypeScript cache
tsconfig.tsbuildinfo

# Vite
dist

# Claude Code local settings
.claude/

# Internal documents
*.pdf

# iCloud "Desktop & Documents" sync duplicates (e.g. "foo 2.js", "build 2")
# This repo lives under ~/Documents/CW&T; iCloud periodically clones files with
# a " 2" suffix. Never track them — they corrupt builds and bloat commits.
* [0-9]
* [0-9].*
